<UserControl x:Class="RTS.Views.ToolsSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Verzio: v0.6.0 - 2026-09-14
         DRAFT - a B2 doboz ("B2: ESZKOZOK") tartalma. CSAK a mar korabban
         elmentett hardware-info.json-t olvassa be (nincs eleve lekerdezes
         induláskor - lasd specifikacio: "NEM minden induláskor"). -->
    <Grid Margin="8">
        <TextBlock x:Name="TxtSummary" TextWrapping="Wrap" FontSize="10" Foreground="{DynamicResource TextBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
    </Grid>
</UserControl>
